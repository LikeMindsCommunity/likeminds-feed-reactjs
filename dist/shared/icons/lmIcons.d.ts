export declare const renderLike: ({ likeColor, defaultColor, height, width, likeStatus, }: RenderLike) => void;
interface RenderLike {
    likeColor: string | undefined;
    defaultColor: string | undefined;
    height: number | undefined;
    width: number | undefined;
    likeStatus: boolean;
}
export {};
