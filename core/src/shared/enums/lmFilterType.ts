export enum FilterType {
    POST = "post",
    COMMENTS = "comments",
    REPLY = "reply",
    PENDING_POSTS = "pending_posts",
  }
  
  export enum LMFeedReportStatus {
    APPROVED = "approved",
    REJECTED = "rejected",
  }
  